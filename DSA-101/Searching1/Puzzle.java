public class Puzzle {

    /* 
     * Given two bags containing locks and keys which have a one to one maping with each other,
     * Find an way to match all pairs of locks and keys.
     * 
     * Note:
     *      Each key can have three states for an lock;
     *      1. lose -> hole > key
     *      2. fit -> hole == key
     *      3. tight -> hole < key
     * 
    */

    public static void main(String[] args) {
        
    }
}
